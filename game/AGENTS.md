# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Durable project decisions

- The HAVEN main lobby includes an optional, independent tower-defense operation led by tactical controller RHEA. Keep its entry visually subordinate to the main sortie action. Deterministic authority stays in `src/defense/engine.js`; Phaser owns only loading, input, sprite presentation, and effects, while React owns the stage selector, tactical HUD, and result/reward surfaces. The active launch set is three sequential stages (`헤이븐 외곽선`, `중계망 정전`, `소버린 야간 공성`) with escalating 6/8/10-wave budgets, twelve stage-specific construction pads, three stage-specific invasion routes, persistent core HP, early-wave start, and first/repeat campaign-currency rewards. `src/defense/battlefields.json` is the shared source for route, core, and pad coordinates; enemies must advance by arc-length sampling on those polylines rather than steering offsets or corner cutting, and generated raster lanes/pads must be rebuilt from that same data with `scripts/build-defense-battlefields.py`. A save slot's first `헤이븐 외곽선` attempt must suspend the simulation and show RHEA's four-step spotlight guide for core HP, construction pads, tower roles, and wave launch; persist `defenseGuideSeen` after completion or skip and never consume Space behind the guide. Keep the tactical HUD core-first, the route/pad cues readable, and the command dock inside desktop and portrait-mobile safe areas. Defense combat labels must remain at least 11px on desktop and 12px on portrait mobile, with tower/action names at least 14px/13px respectively and tutorial body copy at 15px; do not regain playfield space by returning them to 5–10px microcopy. Its four authored defenses remain mechanically distinct: fast single-target PULSE SENTRY, chaining ARC RELAY, long-range splash SKYFIRE BATTERY, and slowing area AEGIS BASTION. Desktop defense keeps its native 1280×720 battlefield; every portrait viewport uses the independently composed 720×1280 battlefield and fixed authority-to-display transform, even if a WebView reports a fine pointer. Load only the selected stage's full or PERFORMANCE battlefield plus the 6×4 tower, role-specific enemy, and combat-VFX atlases under stable texture keys, never multiple stage maps or both tiers in one run. Enemy destruction and tower impacts are presentation-only and must consume stable engine effect/event IDs without moving damage authority into Phaser tweens.
- The active browser runtime is Phaser 4.2.1 with TypeScript and Vite. `src/phaser/` owns boot/loading, the battle scene, WebGL rendering, forward-scrolling route presentation, camera orchestration, pointer/keyboard/touch input, sprite pooling, telegraphs, and combat VFX. React remains responsible for the intro, minimal in-canvas DOM HUD, cinematic dialogue panel, reward modal, accessibility labels, and result screen. Keep the deterministic rules in `src/swarm/engine.js` behind `SceneBridge`; do not move combat authority into Phaser sprite or tween state.
- Treat `src/game/assets/manifest.ts` as the only active runtime asset-path registry. The previous custom Canvas renderer in `src/swarm/renderer.js` may remain for provenance and tests, but the active App must not call `renderSwarm` or mount a hand-authored combat canvas unless the user explicitly asks to roll back the Phaser migration.
- The user's latest explicit replacement direction always supersedes earlier core-loop proposals. The active game is `HUMAN OVERRIDE: OVERLOAD`, a single-character forward-expedition survivor campaign. Regional route budgets are 300/1,000/1,000/1,100/1,150/1,200 for sectors 01—06 before each independent boss stage. The first route also contains ROOK/NYX/MOSS traces. Historical `survivor/` and `adversarial/` arena prototypes were removed from active source and tests during cleanup; do not recreate or reconnect them unless the user explicitly asks to restore a historical mode.
- Keep one controllable player actor on screen. Movement is WASD or arrow keys, Space dashes, and pointer movement aims. The basic weapon always uses continuous pointer-directed automatic fire. There is no automatic-fire toggle, `E` fire-toggle binding, pointer-click manual volley, or equivalent touch toggle; `E` is reserved for AEGIS WARD. Do not restore the discarded fire modes unless the user explicitly reverses this decision.
- Campaign formation contains two playable leads: silver-haired AEGIS and pink twin-tail MIKA, but a new or migrated slot starts with AEGIS only. MIKA is locked until that slot clears `wrong-engine-core`; the first clear plays the one-time four-line visual-novel recruitment scene with the current RHEA/MIKA/AEGIS character art centered on the stage and a full-width bottom dialogue deck, stores `mika-unlocked`, and then exposes her in the character rail, sortie formation, and combat tag reserve. Space/Enter and the large dialogue action advance exactly one line. Never show or accept a locked MIKA selection, including from stale save data or direct launch injection. The selected starter persists per save slot. `T` or the touch TAG control swaps the active actor after a 10-second cooldown while preserving shared hull, XP, level, and encounter state; the incoming actor receives only a short 0.52-second transition guard. Never render or directly control both characters simultaneously. AEGIS uses the selected pulse rifle or beam sword and its matching build tree. MIKA starts with one finite-cadence, non-piercing halo blade; her dedicated five-rank `haloMatrix` level-up augmentation alone grows that basic attack into the former twin-blade, faster, multi-piercing profile. Her first skill offer must surface one of the four-rank identity skills: `prismTempo` strengthens/pierces halo blades and shortens the manual-skill cycle on eligible halo hits, while `heartGuard` restores MIKA's own prismatic guard from successful manual hits. MIKA must never inherit the rifle-only `multishot`, `airstrike`, or `omegaLaser` rewards. Keep `fireTimers.halo` initialized and finite so MIKA can never regress to one volley per simulation step. Her magenta ring fire and four manual actives retain the dedicated portrait, 8-direction 8×8 atlas, and 6×4 VFX sheet.
- The HAVEN character-information surface is a full-screen game roster view, not a generic facility dashboard. Reserve roughly the left half for the selected AEGIS/MIKA transparent portrait at `object-fit: contain`; the right console owns basic stats, the real character/weapon Q/E/F/R kit, and permanent augmentation tabs; a narrow edge rail switches the saved lead character. Keep the portrait visually dominant at desktop and mobile-landscape breakpoints, preserve functional tab/character/purchase/ESC interactions, and never return the illustration to a low-opacity full-panel background. The permanent-upgrade tab must keep the dedicated generated synchronization-reactor image visible at mobile reading size, run bounded idle orbit/core animations, replay one brief charge pulse after a valid purchase, and honor `prefers-reduced-motion`.
- Each campaign slot persists one sortie main weapon: `pulse-rifle` or `beam-sword`. `beam-sword` is locked until that slot first clears `glass-dune`; stale or forged pre-clear saves must sanitize back to `pulse-rifle`. The first valid clear stores the sword unlock and opens the one-time dedicated sword Q/E/F/R guide before the return cinematic; the sortie loadout can reopen that guide afterward. The airship region detail panel must use a wide command-deck layout that keeps briefing, both equipment cards, and the launch button visible together without requiring desktop scrolling. The saved unlocked weapon is preconfirmed and immediately launchable; choosing another unlocked card updates that saved choice without a redundant second confirmation tap. ILYA owns independent three-rank permanent pulse-emitter and edge-resonator upgrades. The equipped weapon travels through the React launch boundary into the deterministic engine; rifle and sword level-up pools stay mechanically distinct rather than reskinning the same rewards.
- Mobile play is portrait-first. A touch or pen may begin movement from any non-interactive point in the combat surface, which creates a temporary translucent floating analog joystick at that contact origin and feeds continuous `moveX/moveY`; never restore the four-button directional pad or a fixed bottom-left stick. Preserve analog magnitude below the rim, use a deadzone, cap diagonal magnitude at one, release captured input on pointer-up/cancel/lost-capture/blur, and keep Q/E/F/R/SPACE/TAG as independent multi-touch buttons. Portrait touch play automatically aims the equipped weapon at the nearest materialized live threat (boss first during boss combat); desktop mouse/trackpad play remains pointer-aimed. The top-level airship screen uses `strategic-world-map.webp` with coordinate hotspots; only after a cluster is chosen may the inner-network or outer-frontier detail map appear. Ordinary passive boss-body overlap deals no hull damage or stun while the beam sword is equipped and only separates AEGIS from the body; authored charge-pattern hits remain dangerous and must not inherit that immunity.
- Space is a true phase dash with at least 0.46 seconds of damage immunity so expanding boss rings can be crossed intentionally. Manual Q/E/F/R abilities are character- and weapon-specific base-kit actions, never aliases of level-up skills. Pulse-rifle AEGIS uses EMP PULSE, AEGIS WARD, STRATOS RUN, and HELIX TEMPEST. Beam-sword AEGIS instead uses the frequent offensive flying-sword set `SPECTRAL SWORD ARRAY` (6s), `PHANTOM REND` (9s), `IMPERIAL SWORD DOMAIN` (15s), and `HEAVENFALL EXECUTION` (45s), whose giant descending blade and shockwave erase nearby common enemies. MIKA uses `PRISM RICOCHET`, `RIBBON VORTEX`, `COMET DUET`, and `HEARTBEAT CARNIVAL`. All sets expose separate cooldown/ready state and share one controller path with mobile touch buttons. A tag must preserve the outgoing actor's four timers and restore the incoming actor's own bank; PRISM TEMPO may accelerate only MIKA's bank. They must not reuse level-up airstrike/Omega entities, events, timers, ranks, or VFX. Arc, Nova, automatic Skyfall support, and automatic Omega Laser remain separate level-up build systems. The discarded `4-FRONT RECALL`, `queueRecall`, temporary four-companion summon, and NULL SNARE gravity behavior must not be restored unless the user explicitly reverses this decision. Persistent ally build choices remain separate autonomous combat options.
- Use fixed regional enemy totals of exactly 300/1,000/1,000/1,100/1,150/1,200 for sectors 01—06. Every expedition starts with only eight close-range hunter units. A wave is combat-gated only: as soon as the current wave has no living hostile, warn and deploy the next authored wave without requiring AEGIS to walk right or reach any map coordinate. Wave allocations grow 14→22→34→52→79→120→180→220 before the terminal remainder, while live pressure remains capped at 220. The first 12% of each budget is hunter-only, riflemen enter the middle bands, and sniper weight plus simultaneous pressure rise only in later bands. Normal enemy HP uses a global 3× durability multiplier before elite and kill-progress scaling. Every arena enemy must still materialize through one of eight visible compass-distributed SOVEREIGN transit gates with a gate-opening animation and non-damaging spawn delay, never directly in empty air around AEGIS. A distant live hostile must remain authoritative, suspend ranged standoff, and use bounded catch-up pursuit rather than despawning or stopping. There is no route-end boss-door, lock wall, gate popup, or minimap boss-gate icon. Once the selected arena budget is cleared and required authored evidence has triggered, start the finite automatic clear sequence: 1.2 seconds of simultaneous warning graphics/effects, then 1.6 seconds of active-character panic presentation, followed by the independent boss-room transition. WRONG ENGINE resolves the 87.2% MOSS trace from kill progress before that transition; do not restore a manual entry-decision modal or any arbitrary walk requirement.
- Keep the three SOVEREIGN field roles mechanically distinct: hunter drones chase until entering an engine-owned proximity radius, then lock in place for a 0.95-second (0.82 elite) irreversible flashing self-destruct countdown. AEGIS can leave the shown 210/246-unit blast radius, after which the drone still detonates without dealing damage. Suppressor infantry are medium-range riflemen with visible three-to-five-round bursts, and brute platforms are long-range snipers that lock one engine-owned warning lane before firing a high-speed rail shot down exactly that line. Distribute fourteen deterministic-but-randomized healing kits across the route; contact restores hull once and removes the kit. These roles, item positions, damage, and collision timing remain simulation-owned rather than Phaser-owned.
- Preserve the region-specific deterministic late compositions in `REGION_ENEMY_PROFILES`: all regions share the eight-hunter tutorial opening and hunter-only first budget band, then blend into their authored identities. WRONG ENGINE finishes at 50/30/20 hunter/suppressor/brute, GLASS DUNE finishes sniper-heavy at 10/30/60, and ABYSSAL ARCHIVE finishes suicide-drone-heavy at 70/20/10. After 72% route progress, deterministic late slots may add the dedicated `siegeWalker`: a 58-radius, 720-base-HP giant biped with a heavy cannon and its own 6×4 motion atlas. Never place it in the opening or early bands, reskin an ordinary sniper as the walker, or flatten the middle/late bands back into one global modulo sequence; use the region's own 300/1,000+ total budget while preserving growth-capped five-gate spawning.
- Present expedition combat through a player-centered zoomed camera over one authored square arena per region. The active actor remains at the exact screen center during arena and boss combat; stage zoom and the user's mouse-wheel zoom-in factor may change without dislodging that center. Keep one compact two-dimensional tactical minimap at the lower-right on desktop and below the top objective on portrait mobile: its square backdrop is the selected region's performance arena asset and its player, authored traces, bounded live-enemy sample, and active transit gates use the full 4,096×4,096 art coordinate system. Do not restore a next-wave route anchor, route-end gate marker, or outside-canvas telemetry rail; objective, health, XP, boss/enemy status, dash, Q/E/F/R active cooldowns, tag, sound, and this tactical map are the only persistent HUD information.
- The active expedition simulation is a 4,096×4,096 square world with a 144-unit safe perimeter; the independent boss chamber remains 1,920×1,080. AEGIS can move symmetrically north, south, east, west, and diagonally across the entire arena. Expedition progress is an abstract kill-progress measure used for authored traces, overdrive, and narrative sequencing; it never derives from player x/y and never constrains movement. Arena enemies use their authored presentation footprint—not only the smaller damage radius—for deterministic, allocation-free separation through the shared spatial grid; even full 220-unit pressure may not collapse into stacked silhouettes. Eight-unit gate bursts use a readable two-column formation rotated around one of eight compass gates before the separation solver takes over. Normal combat uses a player-centered follow zoom before user wheel adjustment. Runtime arena art is loaded only through the asset manifest from `public/assets/overload/regions/<region>/arena-square-v1.webp`, with 768px performance variants under each region's `performance/` folder.
- Treat every route and its boss chamber as two independent stages. Do not load boss-room art, teleport actors, start encounter dialogue, or activate the boss while route hostiles or required authored traces remain. When those prerequisites are complete, freeze hostile combat, show the 1.2-second multi-layer warning and 1.6-second AEGIS panic beat, then automatically fade the route out and load the independent chamber—there is no entry approval/decline modal. Place AEGIS at x=820 and the selected regional boss at x=1390, then fade the boss stage in. This placement keeps AEGIS centered without exposing an unpainted camera edge and brings most of the giant boss silhouette into the expanded view.
- Escalate each region's finite enemy budget with explicitly warned, kill-gated, growth-capped waves appropriate to its 300-1,200-unit arena. A wave begins automatically after the preceding wave has no living hostile; player position can neither delay nor bypass it. The warning must precede the radial gate burst, remain readable over combat, and be reinforced by red edge treatment and sound. At 48/72/88% clear progress, unlock increasingly strong overdrive tiers that accelerate attacks and culminate in automatic master-scale attacks appropriate to the active character and weapon.
- Enemy kills grant XP. The first reward must occur within 6–10 seconds, but later XP thresholds may be banked into one stronger choice. Enforce a meaningful span of active combat between reward overlays; never open consecutive modals as soon as the previous choice closes. Each offer must contain weapon, skill, and ally options of comparable immediate combat value, and a batched choice may grant multiple ranks at once. The third-card `sentry` ally is the mobile LANCE ESCORT: it follows AEGIS and fires paired piercing shots, never leaves a stationary installation behind on the forward route.
- A fresh slot's deployment scenario must finish before the contextual Q/E/F/R walkthrough begins. Completing or skipping that walkthrough persists its seen flag through a stable callback without destroying, recreating, or rewinding the already mounted Phaser controller.
- Include repeatable offensive skill progression and visible mastery transformations. Arc Cascade, Zero-Point Nova, Skyfall air support, and the Omega Laser must all have real combat effects; long-cooldown ultimates require telegraph, impact, cooldown HUD, and materially higher crowd-clearing power. Master ranks should visibly flood the nearby battlefield with upgraded area attacks.
- HANA's laboratory owns two explicit surplus-resource exchanges in addition to permanent research: after two cleared regions, 6 research data may become 3 equipment parts; after four, 12 research data plus 8 equipment parts may become 1 augmentation core. Availability, affordability, balances, and each transaction persist per slot; never imply that these static exchange rates prove the long-term economy is balanced without external playtest evidence.
- With `beam-sword` equipped, continuous rifle fire is fully replaced by an automatic pointer-facing 120-degree close-range sweep. Its weapon reward pool is exclusively CRESCENT WAVE, TITAN EDGE, FLASH REND, and BLADE STORM; its skill pool adds EDGE RESONANCE and PARRY SHEATH and must never offer the rifle-only scatter/rail/rocket/orbit tree. Sword hit arcs, forward displacement, boss damage, guard recharge, cooldowns, and generated effect records remain deterministic engine state. The rifle retains the established projectile and support tree.
- Load only the selected main weapon's presentation bundle at Phaser boot. `pulse-rifle` owns the 8×8 rifle atlas; `beam-sword` owns the 8×8 sword atlas plus the 6×4/64px NEAREST-filtered sword-effect sheet. Both character sheets use rows SOUTH, SOUTHEAST, EAST, NORTHEAST, NORTH, NORTHWEST, WEST, SOUTHWEST and columns 0–3 ready/move, 4–7 attack. Never preload both weapon bundles in one sortie. Sword slash records and Crescent Wave projectiles address the pixel VFX sheet without stretching a cell, while the rifle alone may show a muzzle flash and positional recoil.
- After each regional swarm is cleared, transition to its giant boss. The first THE WRONG ENGINE encounter uses 560,000 HP for a gentler chapter-one difficulty curve; follow-up bosses may retain larger region-specific budgets. Every boss visibly transforms into dedicated phase-2 armor-break and phase-3 core-meltdown sprites at 70% and 38% HP. Each transformation briefly locks damage, emits repeated high-priority warnings, and escalates projectile density, dual sweeps, ring count, warning speed, rapid charges, and autonomous rage bursts. Each encounter must combine distinctly telegraphed radial fire, sweeps, delayed blast zones, expanding rings, single charges, and a rapid multi-charge that briefly relocks before every rush. If AEGIS sidesteps THE WRONG ENGINE's first-region charge and it reaches the chamber wall, apply a 2.6-second groggy state with a real 2.5× incoming-damage multiplier. Direct contact with the boss body or a charge is a critical hit, briefly stuns movement, dash, and basic fire, and visibly separates the player so the collision cannot silently multi-hit.
- Keep the three boss rotations disjoint. THE WRONG ENGINE owns radial fire, sweep, target bombs, rings, charge, and multi-charge. MIRROR TYRANT owns only `prismLattice`, `solarFlare`, `refractionSweep`, and `mirrorShards`. DROWNED ORACLE owns only `memorySpiral`, `depthCollapse`, `archiveEcho`, and `undertow`. Engine lanes, ordered targets, rotating segments, contracting radii, and pull vectors are the single collision/telegraph geometry; Phaser must render those same mutable objects rather than approximate them with unrelated tweens.
- Every boss supports the engine-owned Shift parry window for unavoidable area or rapid-charge attacks. Below 85% HP an eligible attack can pause at contact for exactly 1.5 real-time seconds while the world advances at 16%; frequency rises from every third eligible attack to every second below 70%, then every eligible attack below 38%. Shift/touch success cancels the attack and boss projectiles and opens a 1.8-second 2× vulnerability; expiry applies the unavoidable hit. At 55/30/12% HP each boss stops its normal rotation, sounds a map-wide siren, and deploys exactly 2/3/4 numbered timed bombs with 12/16/20-second disarm windows. Bomb sprites must remain larger than the 64px boss-room hero and their order numbers must stay above actors in a high-contrast overlay. Only a world-space click on the next number defuses it; a wrong bomb or timeout detonates every live bomb and activates a 9-second armor circuit that reduces incoming boss damage to 16%, with an explicit HUD warning. Keep these rules in `engine.js`, not DOM or Phaser tweens.
- Preserve readable combat feedback at crowd scale: remaining-enemy count, route progress, XP progress, level-up choices, boss entrance, danger telegraphs, weak-point exposure, stun/contact feedback, hit-stop, damage feedback, and victory state must remain legible. Keep the center and lower-middle playfield clear during normal play; only scenario dialogue may temporarily occupy the bottom of the screen while simulation is paused. Dialogue art is an enlarged upper-body crop, not a full-body card.
- Enemy health bars are renderer-only and conditional rather than permanently attached to every hostile. Show one unified red health color for two seconds after damage and while an on-screen enemy is close, under the aim line, elite, or actively attacking; use a red proximity strip for suicide drones and a cyan disable marker for EMP. Batch all bars into one Graphics layer with fixed 30/24/18 CINEMATIC/BALANCED/PERFORMANCE caps and a preallocated top-priority scratch list—never create one Phaser object per enemy or render offscreen bars.
- Resolve every scenario portrait from the current speaker rather than falling back to AEGIS for every line. AEGIS uses `public/assets/overload/hero/survivor-portrait.png`; RHEA and the `OPERATOR` speaker share the existing `public/assets/overload/ui/npcs/rhea-control-officer.png`; HANA, ILYA, and LARK use their own 0/1/2 frames from the existing `public/assets/overload/ui/npcs/haven-npc-portraits-atlas.png`; and every named regional boss, including FORGE COLOSSUS, TEMPEST WYRM, and PALE ARCHON, uses the matching selected-region 3-form boss atlas and current phase. Keep regional boss portrait art lazy-mounted with the dialogue/boss stage, and do not invent or claim separate bespoke NPC images where the original atlas is the source.
- HANA/ILYA/LARK's 3-column dialogue atlas must display exactly one square cell at 0%/50%/100%; desktop sizes the full background to `300% auto` inside the clipped portrait box. The portrait-phone bottom sheet may height-fill that same atlas with `auto 100%` only while its clip width stays narrower than one scaled square cell, so the enlarged speaker rises behind the dialogue without a neighboring character leaking into the panel. The three-state command-button atlas must first trim each source state and fill its 512×160 cell, then render at `300% 100%` with 0%/50%/100% state positions even when a component has its own background shorthand. Region and boss proper names use readable mixed labels such as `오답 엔진 중앙로 · WRONG ENGINE CORE`, `유리 사구 · GLASS DUNE`, and `심해 기록고 · ABYSSAL ARCHIVE` instead of discarding the established English callsign.
- Crossfade the authored regional route textures by real route distance while keeping the broad center corridor traversable. WRONG ENGINE uses its three established sectors plus the expanded reactor-vault sector; every other active region adds its own `route-expanded-v2.webp` late-route sector. The boss chamber remains independent and is never part of the scrolling route. Three authored trace checkpoints pause the first route and open bottom-screen dialogue before the engine chamber.
- Render the boss chamber as one authored 1920×1080 image stretched once across the expanded world. Never use a `TileSprite`, mirroring, or repeated texture motion for the boss room; its asymmetric reactor crown, collapsed gantry, left entry lock, and oval playable floor must remain spatially stable during the encounter.
- All gameplay actors use the unified art under `public/assets/overload/hero`, `enemies`, `allies`, and `boss`. Every player, humanoid, drone, deployable, and boss gameplay sprite is centered in its frame. Rotatable non-player actors retain strict 90-degree true-nadir art. AEGIS instead uses eight separately authored high top-down compass views and never rotates or mirrors the whole image at runtime: SOUTH shows the foreshortened face/front, NORTH the back of the head/back armor, EAST/WEST true side profiles, lower diagonals front-side views, and upper diagonals rear-side views. Never restore a single-facing sprite, three-band mirroring, or whole-player rotation. The dialogue portrait remains the intentional viewer-facing upper-body exception.
- Preserve the current battlefield scale hierarchy: AEGIS is 74 px in the route and 64 px in the boss room; hunter, suppressor, and brute bases are 92/108/138 px with elite amplification; boss phases are 480/560/640 px. Boss entry uses a 0.78 base zoom while keeping AEGIS centered, so the player reads small and the engine remains oppressive; mouse-wheel zoom-in remains clamped and cannot expose outside the authored arena. During automatic fire, moving locomotion takes priority; stationary firing uses restrained ready/muzzle frames, the selected compass row's exact muzzle anchor, an 8 fps run cadence, and at most 2 px visual recoil.
- Use `public/assets/overload/hero/survivor-directional-aim-atlas.png` and `survivor-sword-directional-aim-atlas.png` as the only active AEGIS gameplay presentation atlases. Each is 8×8 with 128px cells; PERFORMANCE uses 96px cells. Keep sprite rotation and flip at zero, quantize the continuous pointer heading to the nearest authored compass row, and keep dash ghosts/hit/death inside that same selected-weapon texture. The retired one-direction `survivor-motion-atlas-v2.png` must not be restored or preloaded. Muzzle flash and projectile origin use the quantized row's compass anchor; the reticle retains the continuous pointer projection. Use priority death > hit/stun > dash > moving > stationary fire > idle so automatic fire cannot destabilize movement. Preserve ImageGen chroma sources under `reference/source-assets/overload/` and keep runtime files display-size optimized.
- Keep the dedicated v2 actor motion sheets split by role and load lifetime. Suicide drone, rifleman, and sniper each use their own 6×4/160px sheet with rows for approach, attack, hit/overheat, and death/self-destruct. Hunter drone, pulse sentry, and suppressor drone each use a 5×4/128px sheet with deploy, idle/move, attack, and hit/recall rows; load only the ally actually chosen at a paused reward screen. THE WRONG ENGINE, MIRROR TYRANT, and DROWNED ORACLE each use a selected-region 6×4/320px sheet: rows 0–2 lock stages 1–3 across idle A/B, windup A/B, attack A/B, and row 3 owns transform, exposed-hit, and defeat frames. Load the selected boss motion only when the automatic clear transition starts. Keep static role sprites and 3-form boss atlases as texture-load fallbacks, preserve renderer-only clip clocks keyed by stable entity IDs, and never move animation timing authority into the deterministic engine.
- Keep the Korean-first DOM combat dock at desktop bottom-center with a visually dominant current/max HP bar that pulses and flashes a danger warning on player damage. Directly beneath it, show SPACE dash plus EMP PULSE(Q), 방벽 전개(E), 항공 지원(F), and 섬멸 모드(R) as readable pictogram icons with the input key overlaid and a clockwise radial cooldown mask; short Korean readiness/target labels may supplement, not replace, the icon. In mobile landscape, use a compact layout that does not cover the tactical minimap or touch movement controls; every active icon remains a real touch button using the same controller queue as keyboard input. The automatic level-up airstrike coordinate banner may appear only once per controller run; later automatic casts must still retain their simulation event, sound, telegraph, and impact VFX.
- Chapter-1 squad trace props must remain smaller than AEGIS's 74 px route silhouette; use the current 54/58/62 px ROOK/NYX/MOSS display sizes and do not restore the former oversized 126–152 px corpses.
- The current fiction is a world ruled by the highly advanced sovereign AI `SOVEREIGN`. Its machine army blocks the last free cities. `THE WRONG ENGINE` is the first discovered regional inference branch, not the final global core. AEGIS advances through authored traces left by ROOK, NYX, and MOSS, destroys that branch, then returns to `HAVEN-09` to pursue the independent `GLASS DUNE` and `ABYSSAL ARCHIVE` branches.
- Campaign progress uses three v2 local save slots and must migrate v1 slots without losing clears or records. A new slot launches `wrong-engine-core`; every victory grants first-clear or smaller repeat research-data/equipment-parts/augmentation-core rewards exactly once per run ID. HANA owns three 3-rank research lines (damage, XP, movement); ILYA owns three 3-rank equipment lines (fire rate, max hull, healing); AEGIS owns three 3-rank permanent augmentation lines (weapon output, hull, reflex) purchased with augmentation cores in the dedicated illustrated enhancement panel. Regional difficulty scalars rise sharply from 1.0 in the first route to 3.35 in the sixth and affect authoritative hostile durability, damage, limited speed, XP, and midboss pressure. Purchases persist immediately in only the active slot and their clamped flat combat bonus object is passed into the next deterministic sortie. React owns save slots, the authored base map, HANA/ILYA/LARK/RHEA portrait dialogue, all upgrade facilities, the first-sortie manual-ability briefing, live Q/E/F/R combat-overlay walkthrough, character/weapon formation, and the airship selector. On the base map, render each NPC as a small facility-localized world figure whose size and placement blend with the authored room background; reserve the large art for opened dialogue. The airship region selector must keep the selected/hovered regional artwork bright and readable: use local text/card backing rather than a full-screen black grade, and let hover or keyboard focus visibly enlarge the matching landscape before confirmation. New and migrated slots default to both `abilityGuideSeen=false` and `combatOverlaySeen=false`; the large screenshot briefing and the suspended first-combat HUD spotlight persist independently, explicit DEV `?debug=1` QA bypasses both, and RHEA can reopen only the briefing without resetting the live overlay. Phaser receives the selected `regionId`, character, weapon, and bonuses; defeat must not grant resources, ranks, or unlocks.
- The HAVEN-09 home screen is a character-lobby surface, not the former clickable-room map. Keep the currently equipped and unlocked AEGIS or MIKA approved original transparent illustration as the central visual, NPC/facility navigation on the left edge, character and sortie primary actions on the right, and research-data/equipment-parts/augmentation-core icon counters in the top-right. The active AEGIS and MIKA lobby sources are `public/assets/overload/hero/survivor-portrait.png` and `public/assets/overload/hero/mika-portrait.png`; frame both as stable, readable three-quarter portraits. The earlier proxy Cubism bundles may remain as archived source material, but must not mount in the active lobby or be described as working Live2D until genuinely independent professional rigs produce clearly visible deformation without AEGIS face seams or imperceptible MIKA motion. Do not fake animation with whole-image sliding/scaling, CSS blush dots, face arcs, color overlays, or visible hit-area silhouettes. Head, chest, left arm, right arm, and legs remain explicit transparent DOM click/touch zones for character-specific speech only. Combat narrative must resolve the current active actor so MIKA dialogue never displays the AEGIS portrait after a tag. Use the authored DOM-only 1920×1080 lobby, research-lab, equipment-workshop, and character-sync backgrounds from `public/assets/overload/campaign/lobby/`; warm them through the scoped base preload group and do not register them as Phaser textures. Keep those backgrounds readable under local panel scrims, use Korean-friendly minimum menu type sizes, and render primary command buttons with CSS states rather than the retired `command-button-states.png` atlas.
- Airship navigation is hierarchical. The first screen selects a three-sector cluster (`01—03`, `04—06`, future `07—09`); the second selects the individual sortie. Clearing all of `wrong-engine-core`, `glass-dune`, and `abyssal-archive` raises a blinking `!` over LARK, but sectors 04—06 remain unlaunchable until the player finishes LARK's milestone dialogue and the slot stores `outer-sector-briefed`. Sectors 04—06 use their own route art, four-frame enemy/midboss identity atlas, non-circular three-form boss atlas, and fixed 1,100/1,150/1,200 budgets. The final reinforcement must fill every remaining budget slot rather than stop at the legacy 1,000-unit schedule. Their route-budget clear must spawn and defeat PRESS WARDEN, THUNDER MANTA, or CHIMERA CUSTODIAN before the existing warning/panic transition can move to the independent final-boss stage. Preserve their distinct engine-owned signatures—`PRESS SLAM`, `ARC VOLLEY`, and `CHIMERA RUSH` respectively—rather than reverting all three to the same scaled ordinary-enemy attack. Every regional victory, including sectors 01—03, enters the NIGHTJAR return-to-HAVEN cinematic before restoring the base UI. Keep 07—09 visible only as a non-launchable future cluster until authored content is explicitly added.
- Repeating player skills must use local sprite, line, particle, and restrained shake feedback; do not use a whole-screen white overlay or camera flash for repeated casts. Suicide drones arm only at close range and use compact 126-unit normal / 156-unit elite blast radii. HAVEN NPC dialogue advances exactly one line per non-repeated Space keydown and closes on Space at the final line; Escape remains the immediate close shortcut.
- Every confirmed regional sortie presents a loading transition while the selected Phaser route boots invisibly in a suspended state. The first clear of sectors 01—03 uses the matching user-provided NIGHTJAR cinematic (`wrong-engine-sortie.mp4`, `glass-dune-sortie.mp4`, or `abyssal-archive-sortie.mp4`); sectors 04—06 currently use their warmed regional poster because no dedicated MP4 has been provided. A repeat clear skips the cinematic after loading begins, keeps the regional poster as a short synchronization surface, and skips only the opening deployment dialogue while preserving later trace and boss dialogue. On a first slot, finish RHEA's ability briefing before this launch beat. Mount only the selected video when one exists with `preload="auto"`, use the warmed regional thumbnail as video poster or poster-only fallback, and expose real Phaser loader progress in the sortie footer. During the transition, dynamic-import Phaser, decode/upload only common plus selected-route textures, and warm only combat DOM portraits/reward icons. Reuse that same suspended game instance when both the media transition and runtime are ready—never destroy and recreate it at `ended`, expose a blank canvas, start simulation early, or load boss-room assets. Honor the global sound toggle for video audio, pause BGM while it plays, keep a user-gesture fallback if autoplay is blocked, and reveal the normal error presentation rather than trapping the run on media/runtime failure.
- Use `public/assets/overload/campaign/squad-traces-atlas.png` for the three first-route casualty/evidence props rather than rebuilding them from Phaser Graphics. Use the authored `campaign/haven-09-base.webp`, top-level `campaign/strategic-world-map.webp`, inner `campaign/airship-region-map-v2.webp`, outer `campaign/outer-frontier-region-map.webp`, `ui/npcs/haven-npc-portraits-atlas.png`, and the route/boss-room/boss-form sets under `regions/glass-dune/` and `regions/abyssal-archive/`. Preserve the ImageGen sources under `reference/source-assets/overload/` and keep every active generated asset and exact prompt recorded in `CREDITS.md`.
- Keep the title screen as a full-bleed presentation of `public/assets/overload/intro/start-screen-key-art.webp`. Only the game title, one-line objective, start action, and compact core controls may sit over the art; do not restore the old enemy montage, telemetry cards, counters, header, or footer.
- Keep renderer-local hit and destruction effects in `BattleView`: armor sparks, enemy bursts, player/boss impacts, phase breaks, weapon blasts, and the final boss burst. These effects may enhance presentation but never replace engine-owned damage, telegraph, or collision geometry, and their concurrent count must remain quality-tier capped.
- Keep ordinary rifle rounds readable at the active camera scale: AEGIS pulse shots use a broad cyan-white bolt with a visible trail, rifleman rounds use a thicker red danger bolt and short trail, and sniper rails remain the largest elongated projectile. Enemy rounds stay fully rendered when onscreen; never regain performance by hiding authoritative incoming damage.
- Use the active 6×4/64px authored pixel sheet `public/assets/overload/vfx/pixel/manual-ability-pixel-atlas.png` for STRATOS RUN/HELIX TEMPEST, and dedicated smoothly filtered 6×1/192px high-detail atlases `public/assets/overload/vfx/manual/emp-pulse-hd-atlas.png` and `aegis-ward-hd-atlas.png` for EMP PULSE and AEGIS WARD. `automatic-skill-pixel-atlas.png` remains for SKYFALL/ARC/NOVA plus OMEGA's compact emitter and endpoint only, and the 6×1 `enemy-death-pixel-atlas.png` for common-enemy destruction. Force NEAREST filtering only on pixel atlases. Draw the authoritative OMEGA line as one continuous quality-capped layered Graphics beam; never restore repeated square beam-core tiles or stretch a cannon, impact, fighter, or rotor. STRATOS always follows exactly three engine lanes, emits bounded renderer-local ground impacts along those authoritative paths even without a target, and HELIX exactly four engine lances. SOVEREIGN spawn gates are not skills; keep their authored high-detail row in `vfx/gates/sovereign-gate-motion-atlas.png` (6×1) and address row 0. Do not restore the three inactive large skill textures to `COMMON_GAME_ASSETS`. Healing kits keep their own 4×1 atlas. All animation is presentation only; the deterministic engine owns warning, damage, spawn delay, item collection, and collision timing.
- Boss mechanic presentation uses `boss-pattern-common-pixel-atlas.png` (6×6/64px), `boss-pattern-regional-pixel-atlas.png` (6×4/64px), and `timed-bomb-pixel-atlas.png` (6×2/64px). Load them only with the selected boss room and force NEAREST filtering. The timed-bomb first row is armed/defuse, the second row is explosion; render the engine-owned order as a separate crisp Phaser text label so it cannot be baked into or blurred with the sprite. Keep full lanes, capsules, rings, target radii, click circles, order, timeout, and damage authoritative in the engine.
- Mobile gameplay is portrait-first and must never be blocked by an orientation guard. Keep deterministic simulation coordinates and combat authority unchanged, but use Phaser `RESIZE` with the phone's native portrait logical viewport and a matching adaptive WebGL projection; never crop a 16:9 `ENVELOP` canvas into the phone. Portrait route presentation uses a 0.46 tactical zoom plus an 80-world-unit forward camera lead, while boss rooms use 0.66; these presentation-only values must show nearby hostiles without changing spawn, aim, collision, or damage. The DOM HUD honors all safe-area insets. Landscape browser play remains supported as a secondary layout.
- On phone portrait, movement owns the otherwise empty combat surface while buttons/dialogs remain excluded from joystick capture. Level-up and result overlays must fit within `100dvh` using bounded internal scrolling; reward cards become a readable vertical list, result actions may stack, and narrative advancement keeps a visible Korean label with at least a 50px touch height. Every victory saves first, shows result evidence and rewards, then consumes a persisted post-victory queue for one-time recruitment, weapon guide, and return scenes; refreshing on the result or unlock scene must not lose or replay a consumed step. HAVEN NPC dialogue uses an edge-to-edge safe-area bottom sheet with 15px-or-larger copy and a full-width action deck; its enlarged single-speaker portrait rises above the sheet and sits behind the copy/action layer rather than being clipped inside the panel. Korean facility/interaction/advance labels never break vertically, and viewports at 380px or narrower stack the actions instead. Hide keyboard-only Space/ESC hints on that portrait-touch surface. Keep the HP bar and six compact active controls above the bottom safe area, and keep the tactical minimap below the top objective without covering combat buttons.
- Phone portrait menus are authored surfaces, never scaled desktop compositions. Keep the title at 58px or larger, primary touch targets at least 44px high, save slots in one readable column, world-map hotspots at least 250px wide with 18px or larger names, and regional detail cards as horizontally snapping single-card pages. Portrait combat fills `100dvh` without letterboxing, keeps the objective at 16px or larger, and shows the Korean ability names beneath the bottom icons. The generated 3×3 mobile menu atlas is the active icon source for currencies, NPC facilities, operative management, sortie, and base defense; preserve the SVG fallbacks for load failure. These presentation values must not alter the deterministic simulation, collision, spawn, aim, or damage rules.
- Treat the portrait mobile campaign shell as a native-game information architecture rather than a desktop breakpoint. HAVEN stacks a full-width identity header and labeled three-currency row above the character, exposes its four NPC facilities through a 64px horizontally scrolling rail, and keeps `전투원`/`출격` plus the subordinate full-width `기지 방어` command in a bottom safe-area deck. Facility screens are single-column scrolling sheets. Character information is ordered as top command bar, dominant character art, horizontal roster, then a separately scrolling profile/upgrade console with a 2×2 active-kit grid. Portrait defense uses two readable top status rows and a 2×2 tower palette; do not reintroduce the 5–10px height-compressed desktop labels. Ordinary mobile campaign body text stays at 13–14px or larger and primary actions stay at 48px or larger.
- Phone portrait must explain its own touch controls instead of showing WASD/mouse copy. Keep a visible 48px-or-larger pause control beside sound, single-column pause actions with unbroken Korean labels, and an explicit swipe affordance above horizontally snapping regional cards. Optional vibration feedback may acknowledge dash, tag, active skills, pause, and resume only on coarse-pointer devices; unsupported browsers must remain a silent no-op.
- Treat fine-pointer desktop and coarse-pointer phone layouts as independent authored interfaces, not one height-scaled composition. A short desktop window may compact spacing but must retain readable desktop typography; height-only media queries must not reduce PC campaign text to phone-scale labels. On portrait phones, keep Korean headings and action names at word boundaries, use `2 + 1 full-width` lobby commands, render sortie intel and the unlocked-character list in one column, and keep the sticky sortie confirmation at full panel width. The mobile sortie dialog is viewport-fixed so horizontal card scrolling can never displace it outside the safe area.
- Runtime raster assets must stay display-size optimized. Preserve full-resolution sources under `reference/source-assets/` instead of loading them into the game or copying unused images into the production build.
- Phaser remains combat-entry dynamic-loaded. `BootScene` may register only `COMMON_GAME_ASSETS` plus the selected route returned by `getGameAssetsForRegion(selectedRegionId)`; load `getBossGameAssetsForRegion(selectedRegionId)` only when the automatic clear warning/panic transition begins, never while hostiles remain. The chapter-1 trace atlas is wrong-engine-only and missing regional IDs fall back safely. Preserve the non-overlapping Vite chunks for `phaser-vendor`, `overload-runtime`, `overload-view`, `overload-simulation`, and `overload-assets`; do not collapse them back into the former approximately 1.8MB `createOverloadGame` chunk or modulepreload Phaser on the title screen.
- Maintain adaptive `CINEMATIC / BALANCED / PERFORMANCE` presentation tiers with live frame-time hysteresis while the authoritative simulation remains a deterministic 60Hz fixed step. Desktop keeps the logical/CSS game coordinate space at 1280×720 and uses CINEMATIC 1280×720 at 60fps, BALANCED 1050×590 at 45fps, and PERFORMANCE 896×504 at 30fps. Portrait touch combat instead derives its logical projection from the live phone viewport and multiplies both backing dimensions by the same tier render scale, so quality changes can never restore a 16:9 projection or black side area. A lower render cadence must never reduce input sampling, combat ticks, collision frequency, spawn timing, cooldown timing, or boss geometry updates.
- Resolve the raster asset profile once at Phaser boot. Touch-optimized combat and every portrait defense runtime use the PERFORMANCE asset profile even when the device CPU/GPU tier is higher, because the authored low-memory cells already exceed their phone display size; desktop uses PERFORMANCE only when device detection selects it. PERFORMANCE uses only the matching low-memory paths from `src/game/assets/manifest.ts`; full and performance variants must retain identical texture keys, atlas rows/columns, animation semantics, and world-space presentation. Never load both profiles into one game instance or swap atlas dimensions during a run. Regenerate performance derivatives only with `scripts/build-performance-assets.py`, which resizes project-original atlases cell-by-cell with PIL LANCZOS and derives route/boss images deterministically. Do not use ImageGen or external art for these derivatives, and do not hand-edit generated variants without updating the reproducible script.
- Cull invisible actors, projectiles, pickups, text, shadows, particles, and cosmetic effects before Phaser object/Graphics work. PERFORMANCE may sample actor motion and purely cosmetic VFX less often and use smaller caps, but authoritative sniper lanes, boss telegraphs, danger geometry, HUD, minimap, aim, and current player/enemy state must remain live. Never stride-cull an on-screen hostile, an incoming enemy projectile, or any of the five authored spawn gates; invisible damage or an enemy materializing from empty space is not an acceptable optimization. Segment/ring warnings that cross the viewport stay visible even when their origin is offscreen. Reuse renderer scratch collections and spatial buckets; avoid per-frame arrays, Sets, sorts, gradients, direction objects, or quality-object spreads in crowd-scale paths.
- Pause and reset adaptive frame sampling across window blur, document hiding, Phaser pause/sleep, and WebGL context loss; resume with a clean timing baseline on focus/visibility/context restore so suspension gaps cannot trigger false downgrades. Context recovery must preserve the active asset profile and deterministic simulation state.
- Do not duplicate Phaser textures through React `new Image()` preloads. A small DOM-only preload cache may decode the next screen's title/base/NPC/map/tutorial/reward art before that surface mounts, with an explicit loading screen as fallback, but it must use scoped groups rather than all `DOM_PREVIEW_ASSET_PATHS` and must never include Phaser common/route/boss texture groups. Keep the title key art as an HTML high-priority preload. User-provided BGM must still use metadata-only preload until a user gesture starts playback.
- Pointer aiming, the exact directional muzzle anchor, and the rendered reticle must use the same uniform letterbox transform, clamped camera center, and wheel-adjusted zoom. Reproject a stationary screen pointer after camera-follow or zoom updates so aim never drifts near arena edges, after resize, or when the user zooms in.
- Preserve the near-black industrial arena, cyan player and navigation language, amber reward and weak-point language, and red enemy-danger language. Avoid generic dashboard styling; the canvas combat view is the dominant surface.
- Use the network-free Korean-first DOM font stack `Pretendard Variable`, Pretendard, `Noto Sans KR`, `Apple SD Gothic Neo`, `Malgun Gothic`/`맑은 고딕`, system-ui, and platform sans fallbacks for all Korean prose, labels, buttons, and dialogue. The bundled Rajdhani and IBM Plex Mono packages deliberately import Latin subsets only: Rajdhani is limited to Latin brand/display accents, while IBM Plex Mono is limited to Latin telemetry, codes, keys, and numeric data. Never route Korean glyphs through those Latin-only faces or add a network font dependency.
- Keep every user-provided BGM byte-identical and screen-scoped: `under-ashen-skies-title.mp3` on the title, `last-light-in-haven-09.mp3` in HAVEN-09, `overload-main-theme.mp3` in WRONG ENGINE combat, `refraction-war-glass-dune.mp3` in GLASS DUNE combat, and `memory-below-pressure-abyssal-archive.mp3` in ABYSSAL ARCHIVE combat. Load only the active track with metadata-only preload, pause lobby music during sortie video, and start only the selected region track after that video ends. Sound effects remain project-original and procedurally synthesized with Web Audio.
- Character dialogue stays text-only and browser `speechSynthesis`/voice cloning remain forbidden. The only voice exception is the four committed Korean onboard-agent MP3s for successful Q/E/F/R activation, generated offline with the official Google Cloud Text-to-Speech `ko-KR-Chirp3-HD-Kore` voice and registered through `AGENT_VOICE_PATHS`. Keep each callout to one short activation acknowledgement only; do not append an explanation of the tactical effect. Never expose Google credentials to Vite/browser code or synthesize at runtime. Regenerate only through `scripts/generate-google-agent-voice.mjs` or an authenticated Cloud Shell equivalent, verify byte size/hash/playback, and trigger only from successful `manualAbilityActivated` events. The voice bus must follow the global sound toggle, survive React Strict Mode remounts, and keep R above F/E/Q interruption priority. If the files are unavailable, stay silent rather than restoring browser TTS.
- Write Korean UI copy as natural, concise Korean rather than literal technical translation. Prefer player actions such as `고르세요`, `막아내세요`, `불러오는 중`, `처치`, and `저장` over stacked nouns or system-like phrases such as `전송 중`, `소거 확인`, `회선 동기화`, and `편성 확정`. Preserve established mixed Korean/English proper names and intentional machine dialogue, but keep ordinary buttons, guidance, result text, and human dialogue conversational and easy to scan without changing gameplay rules.
- During the current time-limited polish phase, do not run the full `npm test`, production build, Sites suite, or long multi-scene browser matrix after ordinary changes unless the user explicitly asks. Use only the smallest focused static/runtime check needed for the changed contract. Run the complete verification stack once, at the end, when the user requests final validation.
- Treat the user-provided 2026-08-21 mobile-game screenshots as a minimum physical-scale reference, not a visual style to copy. On phone portrait, ordinary body copy targets 15-16px, core HUD labels 18px or larger where space permits, primary actions 56px or taller, and persistent navigation/touch controls 56-72px. Never shrink these surfaces to preserve a desktop-style one-screen composition; use vertical scrolling, horizontal snap rails, and staged disclosure instead.
- Treat the root `README.md`, this file, `README.md` in `game/`, `design-qa.md`, `CREDITS.md`, the TTS/BGM notes, the mobile release roadmap, the three sources under `docs/project/` (including the content-design baseline), and their generated PDFs as one documentation set. When active rules, asset lifetimes, repository visibility, public URL, test baseline, or platform support changes, update every affected summary rather than leaving an older contract in a portfolio document. Historical exact prompts in `CREDITS.md` must remain verbatim; add an explicit current-runtime note instead of rewriting provenance. Regenerate all three PDFs with `scripts/build-project-pdfs.py` after editing their Markdown sources and visually inspect the rendered pages.
- The portrait P1/P2 presentation pass keeps the wide 0.34 route camera lens, then applies a renderer-only 1.34 actor silhouette multiplier and up to five edge threat arrows; it must never change engine radii, positions, spawn timing, aim, or damage. Portrait combat ability names and live cooldown states stay at 13px/12px or larger inside 60px-or-taller controls, and the wave objective uses the short non-ellipsized Korean label `웨이브 적 섬멸`. Campaign dialogs trap and restore keyboard focus while their background is inert. HAVEN and region horizontal rails expose arrows, dots, and a numeric position; character information keeps its dominant portrait by default but offers a mobile-only compact toggle and automatically compacts when permanent upgrades are opened. Sortie video remains poster-backed and uses `preload="metadata"`; runtime boot progress, not eager MP4 transfer, is the readiness authority. Keep these late responsive rules isolated in `src/styles/p1-p2.css` instead of adding more scattered overrides to the legacy `src/styles.css`.
